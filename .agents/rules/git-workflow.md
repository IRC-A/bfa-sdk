# Git Branch Workflow Rule

- **Always create a new branch**: Before making any code modifications to fix a bug or introduce a feature/enhancement, ALWAYS check out a dedicated git branch (e.g. `fix/<description>` or `feature/<description>`).
- **Protect main branch**: Never apply code changes directly to `main` branch.
